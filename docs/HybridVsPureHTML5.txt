== What the html5 client can and can't do without hybrid enhancement ==

||= Feature =||= Pure HTML5? =||= Comments =||
|| Filesystem access || no || html5 filesystem very limited - does not provide access to user regular files, system files ||
|| Providing necessary native UI operation || limited || jQuery mobile goes pretty far, but? ||
|| Cross browser capability || sketchy || would have to examine prospects ||
|| Settings and persistent data || easy || html5 local storage. ([#lawnchair Lawnchair]?) ||
|| Offline operation || easy || html5 cache manifest ||
|| Circumventing CORS restrictions || depends || may be enough to colocate code in repository domain, but we may require substantial browser widget control ||

> ([=#lawnchair lawnchair] has a bunch of local storage adapters, for
> identical persistent key/value operation across available storage modes
> of most browsers we would need to consider.)
